import Foundation

enum PhysicsModuleTemplateRuntime {
    static let moduleName = "PhysicsModuleTemplate"

    static let overview = """
    Starter physics module scaffold.

    This is intentionally not a polished behavior module. It exists as a launch pad
    for new physics experiments that still fit the engine's current per-particle,
    optimization-driven interaction workflow.
    """
}
